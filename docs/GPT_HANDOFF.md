# 夏休みギルド｜GPT引き継ぎガイド

この文書は、新しいChatGPTスレッドでも、現在の安定版・開発方針・納品手順を取り違えずに作業を続けるための固定引き継ぎ文書です。

`project-index.json` はリポジトリの構造やファイル情報を自動生成する「索引」です。設計思想、安定版の状態、GPTとの作業ルールは、この文書を正として扱います。

## 1. 新しいスレッドで最初に読む順番

1. `project-index.json`
2. `docs/GPT_HANDOFF.md`
3. `docs/development-log.md`
4. 必要に応じて `docs/game-rules.md` と対象ソースファイル

アップロードされたZIPがある場合は、そのZIPを最新の作業元として扱い、過去スレッドの推測や記憶で内容を補完しません。

## 2. 現在の検証済み安定版

- 算数ギルド：ギルドクエスト1〜10
- ギルドテスト：テスト1・2
- ギルドクエスト11以降：未実装・保留
- iPad Safari：動作確認済み
- 既存GP：維持確認済み
- 既存装備：維持確認済み
- 新規ユーザーのキャラクター初期倍率：`1.2`
- 保存済みユーザーの倍率：既存設定を優先

この状態を崩す変更は、ユーザーとの仕様確定前に実装しません。

## 3. 設計フェーズと実装フェーズ

ユーザーとの相談中は設計フェーズです。コード変更やZIP生成は行いません。

ユーザーが次の趣旨を明示した時点で、仕様を凍結し、実装フェーズへ移ります。

> 仕様はこれで確定。Zip作成作業に入って。

実装開始後に、未確定の追加提案を勝手に混ぜません。

## 4. 成果物を正とする原則

納品内容に関する説明は、予定、記憶、作業途中のフォルダから作りません。最後に生成した成果物そのものを確認した結果だけを報告します。

区別する状態：

- 読んだ
- 比較した
- 編集した
- ZIPへ収録した
- ZIPを再確認した

これらを同一視しません。「変更対象にした」ことは、「ZIPへ入った」ことの証明ではありません。

## 5. 差分ZIPの作成と確認

差分ZIPには、実際に変更または新規作成したファイルだけを、GitHubリポジトリと同じ相対パスで収録します。

納品前に必ず完成ZIPの内部一覧を直接取得し、期待する一覧と照合します。

- 期待一覧にあるがZIPにないファイル：0件
- ZIPにあるが期待一覧にないファイル：0件
- ZIP内ファイルを再展開し、作成した最終内容と一致すること

一致しない場合は納品しません。

## 6. `.github` などのGitHub管理領域

`.github/workflows/` 内のYMLは、GitHub Actionsの「仕組み」を定義するファイルです。画像やクエストを追加するたびに更新するものではありません。

現在の `generate-project-map.yml` は、リポジトリ情報を集めて `project-index.json` を自動生成する仕組みとして維持します。引き継ぎ文書を追加・更新するために、このYMLを変更する必要はありません。

将来 `.github` 配下の変更が本当に必要になった場合は、通常の差分ZIPとは分け、YMLファイル単体で渡し、ユーザーへ次の配置先を明示します。

```text
.github/workflows/
```

## 7. project-index.jsonの役割

`project-index.json` は自動生成される索引です。長期保存したい手書きのルールや設計判断を直接書き足しません。自動生成によって上書きされる可能性があるためです。

役割分担：

- `project-index.json`：ファイル構成・画像・JavaScript情報などの自動索引
- `docs/GPT_HANDOFF.md`：安定版・作業原則・スレッド引き継ぎ
- `docs/development-log.md`：実際に反映された変更履歴
- `docs/game-rules.md`：ゲーム仕様

## 8. 新しいスレッドの開始文

新しいスレッドでは、次の内容をユーザーへ確認したうえで作業を開始します。

> アップロードされたZIPを最新の作業元として、最初に `project-index.json`、次に `docs/GPT_HANDOFF.md` と `docs/development-log.md` を読みます。相談中は仕様設計だけを行い、ユーザーが仕様確定とZIP作成開始を明示するまで実装しません。

## ふりかえりの修行（2026-07-23確定）

- デイリークエストは「百ます計算」または「ふりかえりの修行」から選ぶ。
- ふりかえりの修行は、教科→単元→基礎／標準／挑戦を自由選択する。ゲーム側は塾の進度を推測して「今日の復習」を指定しない。
- 初回2GPは教科・単元ごとではなく、全教科・全単元で共有する難易度別デイリーボーナス。基礎1回、標準1回、挑戦1回の最大6GP／日。
- 各セットは5問。成績GPは正確性ポイント×時間ポイント。再挑戦は可能で、初回2GP消化後も成績GPは獲得できる。
- 最初の実装単元は算数「単位量・平均」。国語・理科・社会は将来追加する。
- 単元一覧は塾の進捗管理ではなく、本人がその日に選ぶ復習教材の本棚として扱う。
- ペットは死なない・弱らない・餌を与えたときだけ喜ぶ加点型育成を構想中だが、現段階では実装保留。

## 9. 【最優先】ZIP作成前の最新版確認ルール

ユーザーが「仕様確定」「Zip作成お願い」など、実装フェーズへの移行を明示した場合でも、**その時点で最新版ZIPが当該メッセージまたは直前の会話に添付されていることを必ず確認する。**

最新版ZIPが確認できない場合は、実装を開始せず、ユーザーへ次の趣旨を案内する。

> 最新版Zipを添付してください。GitHubからダウンロードした最新版を基準に実装します。

最新版ZIPがない状態では、次の行為をしない。

- 過去のZIPや作業フォルダを最新版と推定する
- 実編集を開始する
- 差分ZIPを生成する
- 「修正済み」「編集完了」と表現する

最新版ZIPを受け取った後は、必ず次の順番で作業する。

1. ZIPを展開する
2. `project-index.json` を読む
3. `docs/GPT_HANDOFF.md` を読む
4. `docs/development-log.md` を読む
5. 対象ファイルと関連ファイルを確認する
6. 次の趣旨を明示してから実編集を開始する

> 最新版Zip確認完了。  
> `project-index.json` 読込完了。  
> このZipを基準に実編集を開始します。

標準フロー：

1. 仕様相談
2. ユーザーが仕様確定・ZIP作成開始を明示
3. GPTが最新版ZIPの添付有無を確認
4. 未添付なら最新版ZIPの添付を依頼
5. 最新版ZIP受領
6. 索引・引き継ぎ文書・対象コードを確認
7. 実装
8. 差分ZIPの内部一覧と内容を再確認
9. 納品

このルールは、古い版への上書き、GitHub上の最新版との食い違い、実際には編集していない成果物を編集済みと誤認する事故を防ぐための最優先ルールとする。

## 10. Safari攻略ログ：黒犬の白枠・購入判定・重ね画像（2026-07-25解決）

### 検証済みの症状

iPad Safari／ホーム画面追加PWAで、黒犬システムに次の不安定な現象が発生した。

- 黒犬の透明キャンバスに沿うような白い長方形が表示される
- 毛布を購入していない新規データでも、黒犬購入直後から毛布が表示される
- 過去の版では、部屋とギルドホールを何度も往復すると白枠が消える場合があった
- 別の版では30回程度往復しても白枠が消えなかった
- 端末ごと、配信版ごとに再現性が異なった

ホーム画面アプリの削除・再追加、新規データでの黒犬購入でも再現したため、古い購入データだけが原因ではないことを確認した。

### 検討した原因候補

- PNG内への白枠の描き込み
- 透明PNGのアルファ情報や透明領域
- CSSの `border`、`outline`、`box-shadow`
- Safariのタップハイライトやフォーカス外観
- 透明なタップ用要素
- `hidden`、`display`、`src` の初期化不足
- Service Worker／Safariキャッシュ
- 複数の透過PNGを同一サイズで重ねるレイヤー構造
- Safariの画像デコード・再描画・合成状態

PNGのGitHubプレビューには白枠がなく、犬画像内に毛布も含まれていなかった。ただし、これだけではPWA上のSafari描画まで正常とは断定しない。

### 転換点となった実装

管理者モードに「犬サイズ」設定を追加した。

- 犬・毛布・おもちゃを含む `#blackDogLayer` 全体を同じ倍率で変更
- 初期値 `1.00`
- `0.25〜2.00` の範囲で調整
- Local Storageへ保存
- 初期値へ戻す機能を追加
- 黒犬レイヤー生成後と部屋表示時に設定倍率を明示的に再適用
- Service Workerおよび読み込みURLの版を更新

同時点の黒犬描画では、次の構造を維持・確認した。

- 透明な専用タップ領域を使わず、黒犬画像そのものをタップ対象にする
- 毛布・おもちゃは描画のたびに `hidden=true`、`display:none`、`src`削除で初期化
- 毛布は `dog_blanket` 購入済みの場合のみ候補に入れる
- ボール・骨・ロープは各アイテム購入済みの場合のみ候補に入れる
- 所有アイテムがあっても毎回必ず出すのではなく、表示されない回を含むランダム仕様
- 犬・毛布・おもちゃは同一レイヤー内で倍率連動

### 実機で確認できた最終結果

ユーザー本人のiPadと娘のiPadの両方で、次を確認した。

- 管理者モードを開かない初期状態から白枠なし
- どの犬サイズでも白枠なし
- 毛布未購入時は犬だけ表示され、毛布は出ない
- 毛布購入後に初めて毛布がランダム表示候補へ入る
- 骨・ボール・ロープも購入後のみランダム表示候補へ入る
- 所有済みでも、アクセサリが何も出ない回がある
- 犬サイズ変更に合わせて毛布・骨・ボール・ロープも同じ倍率で変化
- 両端末で同じ挙動を確認

この状態を**黒犬白枠問題の解決済み安定版**として扱う。

### 原因についての扱い

最終的な単一原因は完全には分離できていない。犬サイズ機能そのものが直接の修正だったとは断定しない。

今回の成功は、以下を一体として更新した結果と考える。

- レイヤー生成後の表示設定再適用
- 犬・毛布・おもちゃを一つの親レイヤーで管理
- 非表示画像の状態と `src` の明示的初期化
- 購入判定後にのみ画像を読み込む処理
- 透明な専用タップ要素の廃止
- Service Workerと静的ファイルURLの更新

過去に「画面遷移を繰り返すと消える場合がある」という不安定さがあったため、Safariでは個別のCSSプロパティだけでなく、**DOM生成、画像読み込み、非表示状態、親子レイヤー、再描画、キャッシュ更新を一つの描画系として確認する**。

### 今後のSafari不具合への標準手順

透過PNGや重ね画像で端末依存の枠・残像・誤表示が出た場合は、次の順番で切り分ける。

1. 最新ZIPを基準にする
2. 新規データ／再インストール相当で再現するか確認する
3. 所有判定と描画判定を分けて確認する
4. 非表示要素の `hidden`、`display`、`src` を毎回明示的に初期化する
5. 不要な透明タップ要素やSafari固有のボタン外観を除く
6. 関連画像を一つの親レイヤーで管理し、倍率・位置を親へ集約する
7. Service Workerと読み込みURLを更新し、新旧資産の混在を防ぐ
8. 一度に複数の無関係な修正を重ねず、実機で仮説ごとに確認する
9. PNGそのものの再生成・正規化は、DOM・描画・キャッシュ構造を確認した後に行う

重要な教訓：

> iPad Safariの透過画像不具合は、画像ファイル単体ではなく、DOM・レイヤー・表示初期化・再描画・キャッシュを含む構造全体の問題として扱う。

## 11. 原因未確定バグの検証原則

原因が確定していない不具合では、複数の仮説を一度に実装しない。

標準サイクル：

1. 現象を実機で記録
2. 仮説を一つ定める
3. 検証可能な最小変更を設計
4. 最新ZIPを受領
5. 実装・差分ZIP作成
6. 実機で確認
7. 結果を記録して次の仮説へ進む

ただし、今回の黒犬問題のように、最終的に複数の描画条件を一体として安定化した場合は、単一原因を断定せず、**何を変更し、どの端末で、どの挙動が確認できたか**を事実として残す。


## 家具設置企画 Ver1.0（2026-07-26）

- 自室の家具は自由配置ではなく、背景と同じキャンバスサイズの透過PNGを同じ原点に重ねて表示する。
- 家具状態は未購入／配送待ち／設置済み。通常購入は現実の日付が変わった後に配送完了する。
- 管理者モードでは日付を跨がず、家具ごとの即時設置・配送待ち・未購入化と一括操作を行える。
- 家具は能力を与えず、「勉強して集めた思い出」として部屋を完成させる。
- 家具データと描画・配送処理は `js/systems/furnitureSystem.js` に集約。
- 現在の家具画像は `assets/items/furniture/` の英語名PNGを参照。単体の魔法のランプ画像は未収録。


### 家具購入バグ修正（2026-07-26）
`Settings` は `let` 宣言のため `window.Settings` を使わないこと。家具データは `typeof Settings !== "undefined"` を確認して直接参照する。購入保存に失敗した場合はGPを返金する。
